@model Mvc5Test.Models.Genre

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset class="form-horizontal">
        <legend>Genre</legend>

        @Html.HiddenFor(model => model.GenreId)

        <div class="control-group">
            @Html.LabelFor(model => model.GenreName, new { @class = "control-label" })
			<div class="controls">
				@Html.EditorFor(model => model.GenreName)
				@Html.ValidationMessageFor(model => model.GenreName, null, new { @class = "help-inline" })
			</div>
		</div>

        <div class="form-actions no-color">
            <input type="submit" value="Save" class="btn" />
        </div>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

